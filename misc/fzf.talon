tag: terminal
-
fuzzy files: key(ctrl-p)
fuzzy git files:
   key(ctrl-g)
   key(ctrl-f)
fuzzy git hashes:
   key(ctrl-g)
   key(ctrl-s)
fuzzy git tags:
   key(ctrl-g)
   key(ctrl-t)
fuzzy git branches:
   key(ctrl-g)
   key(ctrl-b)
fuzzy git remotes:
   key(ctrl-g)
   key(ctrl-r)
