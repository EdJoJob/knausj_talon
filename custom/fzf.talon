tag: terminal
-
fuzzy files [<user.text>]:
    key(ctrl-p)
    insert(user.text or "")
fuzzy git files [<user.text>]:
   key(ctrl-g)
   key(ctrl-f)
   insert(user.text or "")
fuzzy git hashes [<user.text>]:
   key(ctrl-g)
   key(ctrl-s)
   insert(user.text or "")
fuzzy git tags [<user.text>]:
   key(ctrl-g)
   key(ctrl-t)
   insert(user.text or "")
fuzzy git branches [<user.text>]:
   key(ctrl-g)
   key(ctrl-b)
   insert(user.text or "")
fuzzy git remotes [<user.text>]:
   key(ctrl-g)
   key(ctrl-r)
   insert(user.text or "")
